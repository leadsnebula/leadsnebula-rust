[profile.default]
retries = { backoff = "exponential", count = 2 }
slow-timeout = { period = "60s", terminate-after = "10s" }

[profile.ci]
retries = { backoff = "exponential", count = 3 }
slow-timeout = { period = "120s", terminate-after = "30s" }
